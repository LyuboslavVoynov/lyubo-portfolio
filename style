/* Inspired by Lyubo_Diagram_2.png colors */
:root {
    --bg-dark: #020617;
    --bg-gradient: radial-gradient(circle at top right, #0f172a, #020617);
    --accent-blue: #3b82f6;
    --accent-purple: #a855f7;
    --text-primary: #f8fafc;
    --text-secondary: #94a3b8;
    --glass-border: rgba(255, 255, 255, 0.1);
}

body {
    background: var(--bg-dark);
    background-image: var(--bg-gradient);
    color: var(--text-primary);
    font-family: 'Inter', -apple-system, sans-serif;
    margin: 0;
    line-height: 1.6;
}

.container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 2rem;
}

/* Header & Profile section */
header {
    padding: 5rem 0 3rem;
}

.header-flex {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
}

.subtitle {
    color: var(--accent-blue);
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
    font-size: 0.9rem;
}

.profile-circle {
    width: 180px;
    height: 180px;
    border-radius: 50%;
    border: 3px solid white;
    background: url('your-photo.jpg') center/cover; /* Add your photo here */
    box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
}

/* Glassmorphism Cards inspired by the diagram boxes */
.glass-card {
    background: rgba(15, 23, 42, 0.6);
    border: 1px solid var(--glass-border);
    border-radius: 12px;
    padding: 2rem;
    backdrop-filter: blur(10px);
}

.section-title {
    text-align: center;
    margin: 4rem 0 2rem;
    font-weight: 400;
}

/* Service Grid with Glow Icons */
.service-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
}

.service-box {
    padding: 2rem;
    text-align: center;
    border-top: 1px solid var(--glass-border);
}

.icon-glow {
    width: 50px;
    height: 50px;
    margin: 0 auto 1rem;
    filter: drop-shadow(0 0 8px currentColor);
}

.blue { color: var(--accent-blue); }
.purple { color: var(--accent-purple); }

/* Outcome Stats */
.outcome-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin: 2rem 0;
}

.stat-card {
    background: rgba(255, 255, 255, 0.03);
    padding: 1.5rem;
    border-radius: 8px;
    text-align: center;
}

.stat-value {
    display: block;
    font-size: 1.4rem;
    color: var(--accent-blue);
}

.btn-glow {
    display: inline-block;
    padding: 0.8rem 1.5rem;
    border: 1px solid var(--accent-blue);
    color: var(--accent-blue);
    text-decoration: none;
    border-radius: 6px;
    transition: 0.3s;
}

.btn-glow:hover {
    background: var(--accent-blue);
    color: white;
    box-shadow: 0 0 15px var(--accent-blue);
}

@media (max-width: 768px) {
    .header-flex { flex-direction: column-reverse; text-align: center; }
    .outcome-grid { grid-template-columns: 1fr; }
}
